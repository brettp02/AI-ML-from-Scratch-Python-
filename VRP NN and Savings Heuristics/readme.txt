Before attempting to run, make sure you have the required libraries with the following command:

- pip install numpy && pip install matplotlib

While in the python directory use the following command to run the program:

- python3 main.py